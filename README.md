In order to run the server, please go into the root directory (my-lab) and run the script using this command in your terminal:
./compile_servlet_and_start_winstone.sh

In order to run the test check_g_or_vg.sh, please open another terminal and use the following command:
./chech_g_or_vg.sh

In order to run my two tests (VG-nivå), please use the following command on the client terminal:
./tests.sh
